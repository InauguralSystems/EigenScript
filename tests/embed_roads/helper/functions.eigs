host_scope_clean of "loaded helper"
define go() as:
    return eval of "load_file of \"peer.eigs\""
define direct() as:
    return load_file of "peer.eigs"
define call_file(path) as:
    return host_file of path
