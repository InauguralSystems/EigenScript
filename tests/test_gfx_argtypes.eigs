load_file of "lib/test.eigs"

# #1007 — the argument-type guards in ext_gfx.c.
#
# `Value`'s union overlaps `double num` with `char *str`, and three of the
# file's *_open builtins read `.data.num` with no type check while a sibling
# on the next line (or 180 lines down) DID check. So a string where a number
# belonged reinterpreted a pointer as a double and int-cast it — in practice a
# tiny denormal, so it read as 0 and the call REPORTED SUCCESS:
#
#     gfx_open of ["800", "600", "t"]          -> 1   (a 0x0 window)
#     audio_open of ["44100", "1"]             -> 2   (a device id)
#     audio_capture_open of ["44100", "1"]     -> 3   (a device id)
#
# and the bogus audio opens took the real device with them, so the NEXT
# well-typed audio_open answered 0.
#
# WHY STRICT MODE IS THE LOAD-BEARING HALF. The non-strict stand-in for these
# guards is 0 — which is also what every one of them answers when libSDL2 is
# simply absent. So on a machine without SDL (CI is one) a "wrong types give
# 0" assertion passes on the UNFIXED binary too, and would be pure theatre.
# Under EIGS_STRICT=1 the guard RAISES and names the builtin, which the old
# code could not do from any state, so that half discriminates everywhere.
# The guards are placed ahead of the SDL load for exactly this reason.

define strict_kind(thunk) as:
    k is "none"
    try:
        thunk of null
    catch e:
        k is e.kind
    return k

# The runner runs this file TWICE — once plain, once under EIGS_STRICT=1 —
# and each pass asserts its own mode, because ARG_GUARD's two answers differ:
# non-strict returns the stand-in, strict returns null and raises. Running one
# set of assertions under both modes would fail half of them by construction.
if env_get of "EIGS_STRICT" == "1":
    # The half that discriminates with or without SDL. The old code could not
    # raise from ext_gfx.c at all — `grep -c rt_error src/ext_gfx.c` was 0 —
    # so these rows are impossible to pass on an unfixed binary in any
    # environment, which is exactly what the non-strict rows cannot claim.
    assert_eq of [strict_kind of ((_) => gfx_open of ["800", "600", "t"]), "type_mismatch", "#1007 strict: gfx_open raises on string w/h"]
    assert_eq of [strict_kind of ((_) => audio_open of ["44100", "1"]), "type_mismatch", "#1007 strict: audio_open raises on string freq/channels"]
    assert_eq of [strict_kind of ((_) => audio_capture_open of ["44100", "1"]), "type_mismatch", "#1007 strict: audio_capture_open raises on string freq/channels"]
    assert_eq of [strict_kind of ((_) => audio_stream_open of ["44100", "1"]), "type_mismatch", "#1007 strict: audio_stream_open raises on string freq/channels"]
    # And a well-typed call must NOT raise, or the guard is over-broad and the
    # three rows above would be passing for the wrong reason.
    assert_eq of [strict_kind of ((_) => gfx_open of [80, 60, "t"]), "none", "#1007 strict: a well-typed gfx_open does not raise"]
    # Same control for audio_stream_open. Without it an over-broad guard here
    # — ARG_GUARD(1, ...), a total break of the streaming device — passes every
    # other assertion in this file AND the strict differential, because
    # nothing anywhere calls this builtin with a well-typed argument.
    assert_eq of [strict_kind of ((_) => audio_stream_open of [44100, 1]), "none", "#1007 strict: a well-typed audio_stream_open does not raise"]
    # ...and hand the device straight back. That control OPENS a real stream
    # device, and under the dummy audio driver it holds the only one, so
    # leaving it open made the later `audio_open` answer 0 and silently
    # skipped the whole device-gated block — coverage lost with everything
    # still green.
    audio_stream_close of null

    # The generator family raises too, and the well-typed call must not.
    assert_eq of [strict_kind of ((_) => audio_sine of ["440", 0.01, 0.5]), "type_mismatch", "#1007 strict: audio_sine raises on a string freq"]
    assert_eq of [strict_kind of ((_) => audio_saw of ["440", 0.01, 0.5]), "type_mismatch", "#1007 strict: audio_saw raises on a string freq"]
    assert_eq of [strict_kind of ((_) => audio_square of ["440", 0.01, 0.5]), "type_mismatch", "#1007 strict: audio_square raises on a string freq"]
    assert_eq of [strict_kind of ((_) => audio_sweep of ["100", 200, 0.01, 0.5, 0]), "type_mismatch", "#1007 strict: audio_sweep raises on a string freq"]
    assert_eq of [strict_kind of ((_) => audio_noise of ["0.01", 0.5]), "type_mismatch", "#1007 strict: audio_noise raises on a string duration"]
    assert_eq of [strict_kind of ((_) => audio_envelope of [[0.1, 0.2], "0.01", 0.01, 0.5, 0.01]), "type_mismatch", "#1007 strict: audio_envelope raises on a string attack"]
    assert_eq of [strict_kind of ((_) => audio_gain of [[1.0], "2.0"]), "type_mismatch", "#1007 strict: audio_gain raises on a string volume"]
    assert_eq of [strict_kind of ((_) => audio_sine of [440, 0.01, 0.5]), "none", "#1007 strict: a well-typed audio_sine does not raise"]
    assert_eq of [strict_kind of ((_) => audio_sweep of [100, 200, 0.01, 0.5, 0]), "none", "#1007 strict: a well-typed audio_sweep does not raise"]
    assert_eq of [strict_kind of ((_) => audio_gain of [[1.0], 0.5]), "none", "#1007 strict: a well-typed audio_gain does not raise"]
    print of "strict-pass: 1"
else:
    assert_eq of [gfx_open of ["800", "600", "t"], 0, "#1007: string w/h no longer opens a window"]
    assert_eq of [audio_open of ["44100", "1"], 0, "#1007: string freq/channels no longer opens a device"]
    assert_eq of [audio_capture_open of ["44100", "1"], 0, "#1007: string freq/channels no longer opens a capture device"]
    # audio_stream_open never type-punned — its checks are inside the condition
    # — but a wrong-typed pair silently SKIPPED the override and opened at the
    # 44100/1 defaults, answering a real device id to a caller that asked for
    # something else.
    assert_eq of [audio_stream_open of ["44100", "1"], 0, "#1007: string freq/channels no longer opens a stream device at defaults"]
    # CONTROL. Those three answer 0 for a wrong-typed argument — and 0 is also
    # the no-SDL answer, so on a machine without libSDL2 they pass on the
    # UNFIXED binary too. This row records which of the two worlds the run is
    # in so a green section cannot be misread as evidence about the guards.
    # Deliberately not an assertion: both outcomes are legitimate, and the
    # strict pass above covers both.
    # The generator family. These touch no SDL at all — they compute sample
    # lists from their arguments — so unlike every row above them they are
    # fully discriminating on a machine with no libSDL2, which is where CI
    # runs. Before the guards, `audio_sweep of ["100", ...]` returned 441
    # samples whose first element was a reinterpreted `char *`: an ASLR
    # pointer value, different on every run, handed to the script.
    assert_eq of [len of (audio_sine of ["440", 0.01, 0.5]), 0, "#1007: audio_sine refuses a string freq"]
    assert_eq of [len of (audio_saw of ["440", 0.01, 0.5]), 0, "#1007: audio_saw refuses a string freq"]
    assert_eq of [len of (audio_square of ["440", 0.01, 0.5]), 0, "#1007: audio_square refuses a string freq"]
    assert_eq of [len of (audio_sweep of ["100", 200, 0.01, 0.5, 0]), 0, "#1007: audio_sweep refuses a string freq"]
    assert_eq of [len of (audio_noise of ["0.01", 0.5]), 0, "#1007: audio_noise refuses a string duration"]
    assert_eq of [len of (audio_envelope of [[0.1, 0.2], "0.01", 0.01, 0.5, 0.01]), 0, "#1007: audio_envelope refuses a string attack"]
    # audio_gain is the seventh member, and the one the first pass missed: it
    # was found by RUNNING each candidate twice and diffing, not by reading
    # names. Its sample elements were already type-checked; the volume beside
    # them was not.
    assert_eq of [len of (audio_gain of [[1.0], "2.0"]), 0, "#1007: audio_gain refuses a string volume"]
    # Controls: a well-typed call must still produce samples, or the guards
    # above are over-broad and every row in this block passes for free.
    assert_true of [(len of (audio_sine of [440, 0.01, 0.5])) > 0, "#1007: a well-typed audio_sine still generates"]
    assert_true of [(len of (audio_sweep of [100, 200, 0.01, 0.5, 0])) > 0, "#1007: a well-typed audio_sweep still generates"]
    assert_true of [(len of (audio_envelope of [[0.1, 0.2], 0.01, 0.01, 0.5, 0.01])) > 0, "#1007: a well-typed audio_envelope still generates"]
    assert_eq of [(audio_gain of [[1.0], 0.5])[0], 0.5, "#1007: a well-typed audio_gain still scales"]
    print of ("sdl-present: " + (str of (gfx_open of [80, 60, "eigs #1007 control"])))
    print of "strict-pass: 0"

# ---- the sample-element coercion, when a device is actually available ----
#
# audio_convert_samples used to coerce a non-number to 0, so a wrong-typed
# sample list produced a valid buffer of SILENCE and the caller's `if (!buf)`
# guard never saw it — audio_play answered with a real channel id. This needs
# a live device (audio_play returns 0 on a closed one before it ever looks at
# the samples), so it is gated, and the gate says out loud when it did not run
# rather than counting a pass it never made.
dev is audio_open of [44100, 1]
if dev >= 2:
    assert_eq of [audio_play of [0.1, 0.2, 0.3], 1, "#1007: a well-typed clip still plays"]
    if env_get of "EIGS_STRICT" == "1":
        assert_eq of [strict_kind of ((_) => audio_play of ["a", "b", "c"]), "type_mismatch", "#1007 strict: a wrong-typed sample list raises instead of playing silence"]
    else:
        # With the flag OFF it must answer 0 — audio_play's documented "0 on
        # bad args", which the coercing version could never produce — and NOT
        # hard-error. An earlier draft of this fix raised unconditionally,
        # turning a call that used to return a channel id into a fatal error in
        # the default build. That is the one thing the #971 reform promises not
        # to do, and lib/audio.eigs passes user data straight through to here.
        assert_eq of [audio_play of ["a", "b", "c"], 0, "#1007: non-strict answers 0 for a wrong-typed clip, and does not abort"]
        assert_eq of [audio_play of [0.4, 0.5], 2, "#1007: the mixer still works after a rejected clip"]
    print of "audio-device: 1"
else:
    print of "audio-device: 0"

test_summary of null
