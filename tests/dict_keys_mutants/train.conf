# Release live oracle only; sanitizer harm is gated by tests/test_tsan.sh.
# mutant: structural row; behavioural witness (historical measured accounting)
# fix-reverted, mt-flag-ignored, drain-frees-shared-keys: live key lifetime rows.
# intern-not-under-lock: construction lock/writer rows; no reliable release harm.
# env-name-not-rehomed: construction binding-name guard; ASan UAF, release 0/5.
# strcmp fallback hides intern-node loss; release keys use the rehomed dict array.
ORACLE_TIMEOUT=600
ORACLES=('bash tests/test_dict_keys_mt.sh')
COMPLETED=('^DICT_KEYS_MT: [1-9][0-9]* passed')
MUTANTS=(fix-reverted mt-flag-ignored env-name-not-rehomed intern-not-under-lock drain-frees-shared-keys)
