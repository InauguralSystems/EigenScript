# docs-claims DECLARED POPULATIONS — CLASS | file | expected count
#
# Each count is a FLOOR (#1264), per class AND per file: a count BELOW its row
# is red; a declared row nothing visited is red; an examined pair with no row
# is red. Adding a claim (each is judged on its own) needs no edit here;
# lower a row deliberately when removing claims.
#
# A data file for the same reason as the waiver table beside it: the tool must
# contain no $(cat <<EOF) table at all, because bash 3.2 counts parentheses
# inside a quoted heredoc body and a future row with one would break the macOS
# lane again. Lines beginning with # and blank lines are ignored.
#
NUMBERS|README.md|11
NUMBERS|docs/llms.txt|3
NUMBERS|CLAUDE.md|3
NUMBERS|docs/ARCHITECTURE.md|1
NUMBERS|docs/BUILTINS.md|4
NUMBERS|docs/CONCURRENCY.md|1
PATHS|README.md|101
PATHS|docs/llms.txt|3
PATHS|CLAUDE.md|16
PATHS|docs/ARCHITECTURE.md|6
PATHS|docs/BUILTINS.md|21
PATHS|docs/CONCURRENCY.md|16
FLAGS|README.md|18
FLAGS|docs/llms.txt|9
FLAGS|CLAUDE.md|3
FLAGS|docs/ARCHITECTURE.md|0
FLAGS|docs/BUILTINS.md|4
FLAGS|docs/CONCURRENCY.md|0
TARGETS|README.md|7
TARGETS|docs/llms.txt|0
TARGETS|CLAUDE.md|11
TARGETS|docs/ARCHITECTURE.md|4
TARGETS|docs/BUILTINS.md|5
TARGETS|docs/CONCURRENCY.md|1
NAMES|README.md|4
NAMES|docs/llms.txt|42
NAMES|CLAUDE.md|6
NAMES|docs/ARCHITECTURE.md|1
NAMES|docs/BUILTINS.md|394
NAMES|docs/CONCURRENCY.md|10
NUMBERS|ROADMAP.md|11
PATHS|ROADMAP.md|20
FLAGS|ROADMAP.md|9
TARGETS|ROADMAP.md|2
NAMES|ROADMAP.md|6
#
# docs/CI.md, enrolled 2026-09-21 (round 7 of #1207). Four classes, not five:
# see DOC_FILES_FLAGS_EXEMPT in tools/docs_claims_check.sh for the FLAGS
# exemption and its reason. There is deliberately NO `FLAGS|docs/CI.md` row —
# the class never visits it, and the declaration audit checks both directions,
# so a row here for a class that does not walk the file would be red.
# PATHS 53 -> 63 with #1255: the new "playground: the real wasm32 build, on
# the PR" section and its table row cite pages.yml, web/build.sh,
# tools/ilp32_syntax_check.sh, src/** and web/** (10 path claims, all resolved).
# 63 -> 60 in #1255 round 3: the scope job and its path list were deleted from
# pages.yml, and the CI.md paragraph that listed that path list (and the
# path-filter wording before it) went with them.
# 60 -> 63 with #1264: the "Platform tiers" section (which replaced "Required
# status checks — what is actually required today") cites
# .github/required-checks.txt, tools/ci_tier_check.sh and nightly.yml where the
# old section cited fewer paths (all resolved).
# NUMBERS 4 -> 2 with #1264 PR-B: the hand-stated test-section counts were
# removed from docs/CI.md on purpose (derived numbers only).
NUMBERS|docs/CI.md|2
PATHS|docs/CI.md|63
TARGETS|docs/CI.md|4
NAMES|docs/CI.md|0
