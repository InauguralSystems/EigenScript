# docs-claims WAIVERS — file basename | EXACT line content | reason
#
# A DATA FILE, not a shell string. This one table has now produced three
# quoting traps in three rounds: an apostrophe closed a single-quoted string
# (round 2), an apostrophe closed section_plan's equivalent (round 6), and an
# unbalanced '(' inside a quoted heredoc inside $( ) broke bash 3.2 and took
# down the whole macOS lane (round 10 — bash 3.2 counts parentheses inside a
# quoted heredoc body; bash 5 does not). The content is REVIEWED PROSE quoting
# other people's documents, so it will keep containing apostrophes, backticks
# and parentheses. Data belongs in data: a file has no quoting rules at all.
#
# Lines beginning with # and blank lines are ignored. Everything else is one
# waiver: the basename of the document, the EXACT line content (never a
# substring - mechanical-gates 125), and the reason a reviewer wrote.
#
CLAUDE.md|DMG is 3,288 lines, of which 818 are compiled and 2,470 interpreted,|a measurement of the DMG consumer repo quoted inside a dated bought-lesson narrative; not a fact about this tree
CLAUDE.md|including the 837-line, 128-function opcode dispatch. Every emulated|same DMG narrative as the line above
CLAUDE.md|  EVERY count — `f of []` zero args, `f of [x]` one arg (the element,|metasyntactic call-syntax illustration (#405): `f` stands for any callee, not a named function
CLAUDE.md|  not the list), `f of [a, b]` two. To pass a literal list whole,|same #405 illustration as the line above
CLAUDE.md|  parenthesise (#355): `f of ([x])`. Lint W017 flags the 1-element bare|same #405 illustration as the lines above
CLAUDE.md|  callee re-collects a 2+-element arg list WHOLE (`one of [5, 6]` binds|metasyntactic arity-1 carve-out illustration (#733): `one` is a 1-parameter callee, not a named function
BUILTINS.md|printing "0 rows" forever after a schema change and a migration that did|"0 rows" is quoted program OUTPUT inside a cautionary story, not a claim about this tree
README.md|docker run --rm -v "$PWD:/work" ghcr.io/inauguralsystems/eigenscript:latest program.eigs|`--rm` is docker's flag, not eigenscript's
#
# ROADMAP.md, enrolled 2026-09-21 (round 2 of #1207/#1155). It was NOT in the
# doc set, which is exactly why a typed "112" survived there for a file the
# roadmap gate itself counts at 113. Everything below is either a historical
# release note - true of that release, not a claim about this tree - or a
# figure quoted in order to disown it. Each waiver is the EXACT line, so
# editing the line drops the waiver and the number goes red again.
ROADMAP.md|113 checkbox lines, and 62 checkbox lines of those were historical highlights|the two pre-PR counts, written down once, in the file that describes them, each beside the command that derives it in the block three lines below; nothing else in the repository restates them
ROADMAP.md|- **GitHub Linguist submission.** Its condition (2K+ `.eigs` files across public|GitHub's threshold for its own Linguist submission, quoted; not ours to set, not measured here
ROADMAP.md|  the 500K-cycle canary, EigenMiniSat DPLL/CDCL solves, EigenRegex S1–S12|the DMG consumer's canary length, a parameter of that repo's acceptance command, quoted inside the release-acceptance narrative
ROADMAP.md|  "1253-line" figure this entry used to carry was stale by ~900 lines, and|a stale figure quoted in order to RETIRE it; the same sentence says the measurement is `wc -l src/eigenscript.h`
ROADMAP.md|- 49 stdlib modules, 14 STEM|a historical release note under `## Completed`, true of that release; the current counts are derived in README.md
ROADMAP.md|- 15 STEM simulation examples|a historical release note under `## Completed`, true of that release
ROADMAP.md|- 12 STEM modules|a historical release note under `## Completed` for 0.9.2, true of that release; D_STEM derives TODAY's count, which is a different claim - the words 'standard library' were dropped from this line so the present-tense derivation rule stops claiming a past release note
ROADMAP.md|- UI toolkit — 44 widgets, 3 themes, flex layout, animation|a historical release note under `## Completed` for 0.9.0, true of that release
ROADMAP.md|- Observer semantics, 121 builtins, 25 stdlib modules|a historical release note under `## Completed` for 0.5.0, true of that release
