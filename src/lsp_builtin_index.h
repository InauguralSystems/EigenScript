/* Generated by tools/gen_lsp_builtin_index.sh — DO NOT EDIT (#742).
 * Names come from the registration seams + ext_names.h; hover text
 * from the `name of ...` signature comment above each definition.
 * Reserved report forms are excluded using the lexer keyword table.
 * Regenerated by the Makefile lsp target and build.sh lsp. */
static const char *builtin_docs[][2] = {
    {"abs", "abs — builtin; see docs/BUILTINS.md"},
    {"acos", "acos — builtin; see docs/BUILTINS.md"},
    {"add", "add — builtin; see docs/BUILTINS.md"},
    {"append", "append — builtin; see docs/BUILTINS.md"},
    {"arena_mark", "arena_mark of null — saves current arena position. All Values allocated"},
    {"arena_reset", "arena_reset of null — reclaims all Values allocated since the last arena_mark."},
    {"arena_stats", "arena_stats of null — returns total bytes allocated through the arena."},
    {"args", "args of null → list of command-line arguments (after the script name)."},
    {"asin", "asin — builtin; see docs/BUILTINS.md"},
    {"assert", "assert — builtin; see docs/BUILTINS.md"},
    {"atan", "atan — builtin; see docs/BUILTINS.md"},
    {"atan2", "atan2 — builtin; see docs/BUILTINS.md"},
    {"audio_capture_close", "audio_capture_close of null — stop and close the recording device."},
    {"audio_capture_open", "audio_capture_open of [freq, channels] — open the recording device"},
    {"audio_capture_read", "audio_capture_read of null — drain accumulated samples since the"},
    {"audio_clear", "audio_clear of null — stop all mixer channels and free their clips"},
    {"audio_close", "audio_close of null"},
    {"audio_envelope", "audio_envelope of [samples, attack, decay, sustain_level, release] — ADSR"},
    {"audio_gain", "audio_gain of [samples, volume] — scale and clamp"},
    {"audio_mix", "audio_mix of [samples_a, samples_b] — add and clamp"},
    {"audio_music_play", "audio_music_play of [path, loops] — stream a music file (mp3/ogg/wav) via"},
    {"audio_music_stop", "audio_music_stop of null — halt and free the current track."},
    {"audio_music_volume", "audio_music_volume of v — music volume 0..128"},
    {"audio_noise", "audio_noise of [duration, amplitude] — white noise"},
    {"audio_open", "audio_open of [freq, channels] — open the mixer device (callback mode)"},
    {"audio_pause", "audio_pause of flag — 1=pause, 0=unpause"},
    {"audio_play", "audio_play of samples — convert float list [-1,1] to int16, queue"},
    {"audio_play_loop", "audio_play_loop of [samples, loops] — play `samples` `loops` times on"},
    {"audio_queue_size", "audio_queue_size of null — bytes queued"},
    {"audio_saw", "audio_saw of [freq, duration, amplitude] — sawtooth wave"},
    {"audio_sine", "audio_sine of [freq, duration, amplitude] — generate sine wave samples"},
    {"audio_square", "audio_square of [freq, duration, amplitude] — square wave"},
    {"audio_stop", "audio_stop of channel — stop one mixer channel. Returns 1, or 0 on a"},
    {"audio_stream_clear", "audio_stream_clear of null — drop any buffered audio on the live"},
    {"audio_stream_close", "audio_stream_close of null — stop and close the live stream device."},
    {"audio_stream_open", "audio_stream_open of [freq, channels] — open the live streaming"},
    {"audio_stream_push", "audio_stream_push of samples — queue a block of float samples [-1, 1]"},
    {"audio_stream_queued", "audio_stream_queued of null — samples still buffered (not yet played)"},
    {"audio_sweep", "audio_sweep of [freq_start, freq_end, duration, amplitude, waveform]"},
    {"audio_volume", "audio_volume of [channel, vol] — live per-channel volume, 0.0..4.0"},
    {"bit_and", "bit_and — builtin; see docs/BUILTINS.md"},
    {"bit_not", "bit_not — builtin; see docs/BUILTINS.md"},
    {"bit_or", "bit_or — builtin; see docs/BUILTINS.md"},
    {"bit_shl", "bit_shl — builtin; see docs/BUILTINS.md"},
    {"bit_shr", "bit_shr — builtin; see docs/BUILTINS.md"},
    {"bit_xor", "bit_xor — builtin; see docs/BUILTINS.md"},
    {"buf_copy", "buf_copy of [src, src_off, dst, dst_off, count] — bulk copy between buffers."},
    {"buf_deinterleave", "buf_deinterleave of [src, channel, nch, count?] — every nch-th sample"},
    {"buf_dot", "buf_dot of [a, b, a_off, b_off, count] — windowed dot product:"},
    {"buf_fill", "buf_fill of [b, off, count, value] — bulk store over a window:"},
    {"buf_from_list", "buf_from_list of list — convert list of numbers to buffer"},
    {"buf_from_pcm16le", "buf_from_pcm16le of [bytes, byte_off, count] — decode `count`"},
    {"buf_get", "buf_get of [buf, index] — O(1) indexed read"},
    {"buf_len", "buf_len of buf — return buffer length"},
    {"buf_mix", "buf_mix of [dst, src, dst_off, src_off, count, gain] —"},
    {"buf_peak", "buf_peak of [b, off, count] — max |x| over a window (normalize and"},
    {"buf_resample_linear", "buf_resample_linear of [src, dst_len] — endpoint-inclusive linear"},
    {"buf_scale_range", "buf_scale_range of [b, off, count, gain] — in-place multiply over a"},
    {"buf_set", "buf_set of [buf, index, value] — O(1) indexed write"},
    {"buf_to_pcm16le", "buf_to_pcm16le of [floats, off, count] — encode `count` samples from"},
    {"buffer", "buffer of count — create a zero-filled numeric buffer"},
    {"build_corpus", "build_corpus of [file_list, top_n, stream_path, vocab_path]"},
    {"ceil", "ceil — builtin; see docs/BUILTINS.md"},
    {"channel", "channel — builtin; see docs/BUILTINS.md"},
    {"channel_closed", "channel_closed — builtin; see docs/BUILTINS.md"},
    {"char_at", "char_at of [string, index] → single character as string, or \"\" if out of range."},
    {"chdir", "chdir of \"path\" → 1 on success, 0 on failure"},
    {"chr", "chr of n → single-character string from ASCII code"},
    {"classify", "classify — builtin; see docs/BUILTINS.md"},
    {"clear_math_flags", "clear_math_flags of null"},
    {"clock_unix", "clock_unix of null — seconds since the Unix epoch from CLOCK_REALTIME"},
    {"close_channel", "close_channel — builtin; see docs/BUILTINS.md"},
    {"coalesce", "coalesce of [value, default] — returns value unless empty/null"},
    {"concat", "concat of [list_a, list_b] → new 1D list with a's elements then b's"},
    {"contains", "contains — builtin; see docs/BUILTINS.md"},
    {"copy_into", "copy_into of [dest, dest_offset, src]"},
    {"cos", "cos — builtin; see docs/BUILTINS.md"},
    {"db_connect", "db_connect — db extension builtin; see docs/BUILTINS.md"},
    {"db_execute", "db_execute — db extension builtin; see docs/BUILTINS.md"},
    {"db_query_json", "db_query_json — db extension builtin; see docs/BUILTINS.md"},
    {"db_query_value", "db_query_value — db extension builtin; see docs/BUILTINS.md"},
    {"deflate", "deflate — builtin; see docs/BUILTINS.md"},
    {"dict_remove", "dict_remove — builtin; see docs/BUILTINS.md"},
    {"dict_set", "dict_set — builtin; see docs/BUILTINS.md"},
    {"dispatch", "dispatch of [table, key, arg] — O(1) function dispatch."},
    {"divide", "divide — builtin; see docs/BUILTINS.md"},
    {"dot", "dot — builtin; see docs/BUILTINS.md"},
    {"eigen_checkpoint_info", "eigen_checkpoint_info — model extension builtin; see docs/BUILTINS.md"},
    {"eigen_eval_loss", "eigen_eval_loss — model extension builtin; see docs/BUILTINS.md"},
    {"eigen_generate", "eigen_generate — model extension builtin; see docs/BUILTINS.md"},
    {"eigen_model_info", "eigen_model_info — model extension builtin; see docs/BUILTINS.md"},
    {"eigen_model_load", "eigen_model_load — model extension builtin; see docs/BUILTINS.md"},
    {"eigen_model_loaded", "eigen_model_loaded — model extension builtin; see docs/BUILTINS.md"},
    {"eigen_model_save_binary", "eigen_model_save_binary — model extension builtin; see docs/BUILTINS.md"},
    {"ends_with", "ends_with — builtin; see docs/BUILTINS.md"},
    {"env_get", "env_get — builtin; see docs/BUILTINS.md"},
    {"eval", "eval of code_string — execute EigenScript code and return result"},
    {"exe_path", "exe_path of null → absolute path of the running interpreter binary."},
    {"exec_capture", "exec_capture of [\"cmd\", \"arg1\", ...]               → [exit_code, stdout_text]"},
    {"exit", "exit of N — request a clean process exit with code N (default 0). Sets the"},
    {"exp", "exp — builtin; see docs/BUILTINS.md"},
    {"f64_from_bytes", "f64_from_bytes of <list|buffer of 8 big-endian bytes> → the decoded double."},
    {"f64_to_bytes", "f64_to_bytes of x → list of 8 ints: the big-endian IEEE-754 double encoding"},
    {"file_exists", "file_exists — builtin; see docs/BUILTINS.md"},
    {"fill", "fill of [count, value] — create a list of `count` elements all set to `value`."},
    {"floor", "floor — builtin; see docs/BUILTINS.md"},
    {"flush", "flush of null — flush stdout"},
    {"free_val", "free_val of value → frees a heap-allocated Value tree. Returns null."},
    {"gather", "gather of [tensor, indices, dim] → select elements at indices along last dim"},
    {"get_at", "get_at of [list, index] or get_at of [list, row, col]"},
    {"get_observer_scale", "get_observer_scale of null — the value channel's characteristic scale."},
    {"get_observer_thresholds", "get_observer_thresholds — builtin; see docs/BUILTINS.md"},
    {"get_observer_window", "get_observer_window of null | \"x\" — the default window depth, or the depth in force on binding \"x\"."},
    {"getcwd", "getcwd of null → current working directory as string"},
    {"gfx_circle", "gfx_circle of [cx, cy, radius, r, g, b] — filled circle via midpoint"},
    {"gfx_clear", "gfx_clear of [r, g, b]"},
    {"gfx_clip", "gfx_clip of [x, y, w, h] — set render clip rectangle."},
    {"gfx_close", "gfx_close of null"},
    {"gfx_delay", "gfx_delay of ms"},
    {"gfx_fb", "gfx_fb of [buffer, width, height, x, y, scale]"},
    {"gfx_line", "gfx_line of [x1, y1, x2, y2, r, g, b]"},
    {"gfx_open", "gfx_open of [width, height, title]"},
    {"gfx_point", "gfx_point of [x, y, r, g, b]"},
    {"gfx_poll", "gfx_poll of null — return next event as dict, or null if none."},
    {"gfx_present", "gfx_present of null — flip buffer to screen"},
    {"gfx_read", "gfx_read of [x, y] — read back one rendered pixel as [r, g, b]."},
    {"gfx_rect", "gfx_rect of [x, y, w, h, r, g, b] or [x, y, w, h, r, g, b, a]"},
    {"gfx_rrect", "gfx_rrect of [x, y, w, h, radius, r, g, b] or [..., a]"},
    {"gfx_text", "gfx_text of [x, y, text, r, g, b] or [x, y, text, r, g, b, scale]"},
    {"gfx_text_height", "gfx_text_height of scale? (number, [scale], or null → 1) — pixel line"},
    {"gfx_text_width", "gfx_text_width of [text, scale?] (or of \"text\") — pixel width of `text`"},
    {"gfx_ticks", "gfx_ticks of null — milliseconds since SDL_Init"},
    {"gfx_title", "gfx_title of \"new title\""},
    {"has_key", "has_key — builtin; see docs/BUILTINS.md"},
    {"heap_inuse", "heap_inuse of null — bytes currently in use by the C allocator"},
    {"hex", "hex of n → uppercase hex string, minimal digits (\"0\" for 0)."},
    {"hmac_sha256", "hmac_sha256 — builtin; see docs/BUILTINS.md"},
    {"http_cors", "http_cors of origin — configure CORS. Pass \"*\" for wildcard, null to disable."},
    {"http_early_bind", "http_early_bind — http extension builtin; see docs/BUILTINS.md"},
    {"http_post", "http_post of [url, headers_json, body_string] -> response body string"},
    {"http_request_body", "http_request_body — http_request extension builtin; see docs/BUILTINS.md"},
    {"http_request_headers", "http_request_headers of null → raw request headers as string."},
    {"http_route", "http_route — http extension builtin; see docs/BUILTINS.md"},
    {"http_route_authed", "http_route_authed — http extension builtin; see docs/BUILTINS.md"},
    {"http_serve", "http_serve — http extension builtin; see docs/BUILTINS.md"},
    {"http_session_id", "http_session_id — http_request extension builtin; see docs/BUILTINS.md"},
    {"http_static", "http_static — http extension builtin; see docs/BUILTINS.md"},
    {"index_of", "index_of of [haystack, needle] → first index, or -1. Non-string operands"},
    {"inflate", "inflate — builtin; see docs/BUILTINS.md"},
    {"is_dir", "is_dir of path — 1 if path names a directory, 0 for a plain file, a"},
    {"is_file", "is_file — builtin; see docs/BUILTINS.md"},
    {"join", "join of [list, separator] — concatenate list elements into a string."},
    {"json_build", "json_build of [key1, val1, key2, val2, ...] — properly escaped JSON object"},
    {"json_decode", "json_decode — builtin; see docs/BUILTINS.md"},
    {"json_encode", "json_encode — builtin; see docs/BUILTINS.md"},
    {"json_path", "json_path of [json_string, \"dot.path\"] -> value as string, or \"\""},
    {"json_raw", "json_raw — builtin; see docs/BUILTINS.md"},
    {"keys", "keys — builtin; see docs/BUILTINS.md"},
    {"leaky_relu", "leaky_relu of tensor → element-wise max(0.01*x, x). Works on 1D or 2D."},
    {"len", "len — builtin; see docs/BUILTINS.md"},
    {"list_contains", "list_contains of [list, value] — 1 if any element structurally equals"},
    {"list_index_of", "list_index_of of [list, value] — index of the first element structurally"},
    {"list_insert_at", "list_insert_at of [list, index, value] — insert value at index, shift tail"},
    {"list_remove_at", "list_remove_at of [list, index] — remove element at index, shift tail down."},
    {"list_slice", "list_slice of [list, start, end] → new list with the elements of [start, end)."},
    {"list_truncate", "list_truncate of [list, new_len] — shrink list in-place to new_len items."},
    {"load_file", "load_file — builtin; see docs/BUILTINS.md"},
    {"log", "log of 1e-15 answered ln(1e-10) with no flag, a silent plateau that a"},
    {"log_softmax", "log_softmax — builtin; see docs/BUILTINS.md"},
    {"ls", "ls of \"path\" → list of filenames in directory, or [] on failure."},
    {"math_flags", "math_flags — builtin; see docs/BUILTINS.md"},
    {"matmul", "matmul — builtin; see docs/BUILTINS.md"},
    {"max", "max — builtin; see docs/BUILTINS.md"},
    {"md5", "md5 — builtin; see docs/BUILTINS.md"},
    {"md5_file", "md5_file — builtin; see docs/BUILTINS.md"},
    {"mean", "mean — builtin; see docs/BUILTINS.md"},
    {"min", "min — builtin; see docs/BUILTINS.md"},
    {"mkdir", "mkdir of \"path\" → 1 on success, 0 on failure. Creates parents."},
    {"mktemp", "mktemp of null → path to a new temporary file"},
    {"model_load_weights", "model_load_weights — model extension builtin; see docs/BUILTINS.md"},
    {"model_save_weights", "model_save_weights — model extension builtin; see docs/BUILTINS.md"},
    {"monotonic_ms", "monotonic_ms of null — milliseconds from CLOCK_MONOTONIC"},
    {"monotonic_ns", "monotonic_ns of null — nanoseconds from CLOCK_MONOTONIC"},
    {"multiply", "multiply — builtin; see docs/BUILTINS.md"},
    {"must_not_yield", "must_not_yield of fn — run `fn of null` as an ATOMIC critical section and"},
    {"native_train_step_builtin", "native_train_step_builtin — model extension builtin; see docs/BUILTINS.md"},
    {"nearest_in_range", "nearest_in_range of [entities, x, y, range, world_w, world_h, px_key, py_key, active_key]"},
    {"nearest_in_range_all", "nearest_in_range_all of [entities, range, world_w, world_h, px_key?, py_key?, active_key?]"},
    {"negative", "negative — builtin; see docs/BUILTINS.md"},
    {"net_accept", "net_accept of listener_id"},
    {"net_close", "net_close of handle_id → null. Deterministic and untraced: closing is"},
    {"net_dial", "net_dial of [host, port]"},
    {"net_listen", "net_listen of port → listener handle id, or null when the bind/listen"},
    {"net_port", "net_port of listener_id → the locally bound port (the kernel's pick"},
    {"net_recv", "net_recv of [conn_id, max_bytes]"},
    {"net_send", "net_send of [conn_id, data] — data is a string or a buffer/list of"},
    {"norm", "norm of a → L2 (Euclidean) norm = sqrt(sum_i a[i]^2). Buffers and tensors."},
    {"num", "num — builtin; see docs/BUILTINS.md"},
    {"num_copy", "num_copy of val → fresh heap-allocated copy of a numeric Value."},
    {"numerical_grad", "numerical_grad of [loss_fn, param, eps]"},
    {"numerical_grad_cols", "numerical_grad_cols of [loss_fn, matrix, col_indices, eps]"},
    {"numerical_grad_rows", "numerical_grad_rows of [loss_fn, matrix, row_indices, eps]"},
    {"observe", "observe — builtin; see docs/BUILTINS.md"},
    {"ord", "ord of s → first byte of s as integer (0..255), or -1 on empty / non-string"},
    {"path_base", "path_base of \"a/b/c.txt\" → \"c.txt\""},
    {"path_dir", "path_dir of \"a/b/c.txt\" → \"a/b\""},
    {"path_ext", "path_ext of \"a/b/c.txt\" → \".txt\""},
    {"path_join", "path_join of [a, b] → \"a/b\""},
    {"pi", "pi — builtin; see docs/BUILTINS.md"},
    {"pow", "pow — builtin; see docs/BUILTINS.md"},
    {"ppu_render_frame", "ppu_render_frame of [mem_buf, fb_buf]"},
    {"print", "print — builtin; see docs/BUILTINS.md"},
    {"proc_close", "proc_close of fd                       → null (idempotent on EBADF)"},
    {"proc_read", "proc_read of [out_fd, max_bytes]       → string (raw bytes; NUL-truncates) | null EOF"},
    {"proc_read_buf", "proc_read_buf of [out_fd, max_bytes]   → VAL_BUFFER (binary-safe) | null EOF"},
    {"proc_read_line", "proc_read_line of out_fd               → string (no trailing \\n) | null EOF"},
    {"proc_spawn", "proc_spawn of [\"cmd\", \"arg1\", ...]     → [pid, in_fd, out_fd]  | [-1,-1,-1]"},
    {"proc_wait", "proc_wait of pid                       → exit_code | -1 on error"},
    {"proc_write", "proc_write of [in_fd, \"text\"]          → bytes_written | -1 on broken pipe"},
    {"random", "random of null → float in [0, 1)"},
    {"random_hex", "random_hex of n → string of n random hex characters from /dev/urandom."},
    {"random_int", "random_int of [lo, hi] → integer in [lo, hi] inclusive"},
    {"random_normal", "random_normal of [rows, cols, scale] → 2D, or random_normal of [len, scale] → 1D"},
    {"range", "range of n → [0, 1, ..., n-1]"},
    {"raw_key", "raw_key — builtin; see docs/BUILTINS.md"},
    {"read_bytes", "read_bytes of path — read binary file, return list of byte values (0-255)"},
    {"read_bytes_buf", "read_bytes_buf of path — read binary file, return VAL_BUFFER of byte values."},
    {"read_line", "read_line of null — blocking line read from stdin via getline(3):"},
    {"read_text", "read_text of \"path\" → file contents as string, or \"\" on failure."},
    {"record_history", "record_history of flag — enable (nonzero) or disable (0) per-assignment"},
    {"recv", "recv — builtin; see docs/BUILTINS.md"},
    {"recv_timeout", "recv_timeout of [channel, ms] — bounded wait. Returns the value if one"},
    {"regex_find", "regex_find — builtin; see docs/BUILTINS.md"},
    {"regex_match", "regex_match — builtin; see docs/BUILTINS.md"},
    {"regex_replace", "regex_replace of [string, pattern, replacement] -> string"},
    {"relu", "relu of tensor → element-wise max(0, x). Works on 1D or 2D."},
    {"remove_file", "remove_file of path — delete a file. Returns 1 on success, 0 on failure."},
    {"rename", "rename of [old_path, new_path] — rename/replace a file. On POSIX rename(2) is"},
    {"reshape", "reshape of [buf, rows, cols] -> a shaped copy of the flat buffer (rows*cols"},
    {"rm", "rm of \"path\" → 1 on success, 0 on failure"},
    {"round", "round — builtin; see docs/BUILTINS.md"},
    {"sandbox_run", "sandbox_run of [descriptor, max_iterations?] — run an EigenScript-assembled"},
    {"scan_int_tokens", "scan_int_tokens of text"},
    {"scan_ints", "scan_ints of text"},
    {"scan_tokens", "scan_tokens of text"},
    {"screen_clear", "screen_clear of null — clear terminal and hide cursor"},
    {"screen_end", "screen_end of null — show cursor and reset"},
    {"screen_put", "screen_put of [row, col, char, color_code] — write a character at terminal position"},
    {"screen_render", "screen_render of [entities_list, screen_w, screen_h, player_x, player_y, world_w, world_h]"},
    {"secure_equals", "secure_equals of [a, b] → 1 if the two strings are equal, else 0."},
    {"seed_random", "seed_random of n → seeds the RNG, returns 1"},
    {"send", "send — builtin; see docs/BUILTINS.md"},
    {"set_at", "set_at of [list, index, value] — sets list[index] = value, returns list"},
    {"set_observer_scale", "set_observer_scale of s — set the value channel's characteristic scale (the |v| below which a value counts as zero), s > 0."},
    {"set_observer_thresholds", "set_observer_thresholds — builtin; see docs/BUILTINS.md"},
    {"set_observer_window", "set_observer_window of n | [\"x\", n] — set the default (n) or one binding's ([\"x\", n]) observer window depth, 4..64 samples."},
    {"sgd_update", "sgd_update of [param, grad, lr] — in-place param = param - lr * grad"},
    {"sgd_update_cols", "sgd_update_cols of [matrix, grad, col_indices, lr]"},
    {"sgd_update_rows", "sgd_update_rows of [matrix, grad, row_indices, lr]"},
    {"sha256", "sha256 — builtin; see docs/BUILTINS.md"},
    {"sha256_file", "sha256_file — builtin; see docs/BUILTINS.md"},
    {"shape", "shape of tensor → [rows, cols] for 2D, [len] for 1D"},
    {"shared_clear", "shared_clear — http_request extension builtin; see docs/BUILTINS.md"},
    {"shared_delete", "shared_delete — http_request extension builtin; see docs/BUILTINS.md"},
    {"shared_get", "shared_get — http_request extension builtin; see docs/BUILTINS.md"},
    {"shared_has", "shared_has — http_request extension builtin; see docs/BUILTINS.md"},
    {"shared_incr", "shared_incr — http_request extension builtin; see docs/BUILTINS.md"},
    {"shared_keys", "shared_keys — http_request extension builtin; see docs/BUILTINS.md"},
    {"shared_set", "shared_set — http_request extension builtin; see docs/BUILTINS.md"},
    {"shared_size", "shared_size — http_request extension builtin; see docs/BUILTINS.md"},
    {"sign_extend", "sign_extend of [val, bits] — sign-extend val from given bit width."},
    {"sin", "sin — builtin; see docs/BUILTINS.md"},
    {"softmax", "softmax — builtin; see docs/BUILTINS.md"},
    {"sort", "sort of list — in-place qsort of an all-number or all-string list."},
    {"sort_by", "sort_by of [list, key_fn] — sort list by numeric keys from key_fn."},
    {"spawn", "spawn — builtin; see docs/BUILTINS.md"},
    {"split", "split — builtin; see docs/BUILTINS.md"},
    {"sqrt", "sqrt — builtin; see docs/BUILTINS.md"},
    {"starts_with", "starts_with — builtin; see docs/BUILTINS.md"},
    {"state_at", "state_at — builtin; see docs/BUILTINS.md"},
    {"store_close", "store_close — builtin; see docs/BUILTINS.md"},
    {"store_collections", "store_collections — builtin; see docs/BUILTINS.md"},
    {"store_count", "store_count — builtin; see docs/BUILTINS.md"},
    {"store_delete", "store_delete — builtin; see docs/BUILTINS.md"},
    {"store_drop", "store_drop — builtin; see docs/BUILTINS.md"},
    {"store_get", "store_get — builtin; see docs/BUILTINS.md"},
    {"store_open", "store_open — builtin; see docs/BUILTINS.md"},
    {"store_put", "store_put — builtin; see docs/BUILTINS.md"},
    {"store_query", "store_query — builtin; see docs/BUILTINS.md"},
    {"store_update", "store_update — builtin; see docs/BUILTINS.md"},
    {"str", "str — builtin; see docs/BUILTINS.md"},
    {"str_from_bytes", "str_from_bytes of <list|buffer of byte ints> → string of those raw bytes."},
    {"str_lower", "str_lower — builtin; see docs/BUILTINS.md"},
    {"str_replace", "str_replace — builtin; see docs/BUILTINS.md"},
    {"str_upper", "str_upper — builtin; see docs/BUILTINS.md"},
    {"stream_close", "stream_close of null            → closes the stream file, returns 1"},
    {"stream_open", "stream_open of [\"path\", count]  → opens file, writes header with count, returns 1"},
    {"stream_write", "stream_write of value           → writes one float64, returns 1"},
    {"substr", "substr of [string, start, length] → substring"},
    {"subtract", "subtract — builtin; see docs/BUILTINS.md"},
    {"sum", "sum — builtin; see docs/BUILTINS.md"},
    {"tan", "tan — builtin; see docs/BUILTINS.md"},
    {"task_alive", "task_alive of id → 1 while the task is READY/RUNNING/SUSPENDED, else 0"},
    {"task_detach", "task_detach of id -> 1 (0 for main/unknown). Marks the task fire-and-forget"},
    {"task_join", "task_join of id — block until task `id` finishes; return its (deep-copied)"},
    {"task_kill", "task_kill of id — deterministically tear down task `id`: drop its mailbox"},
    {"task_now", "task_now of null → the current virtual-clock value (a number, 0 before any"},
    {"task_recv", "task_recv of null — return the next message from this task's mailbox, or"},
    {"task_sched_seed", "task_sched_seed of n — install a scheduling seed. By default tasks run FIFO"},
    {"task_sched_trace", "task_sched_trace of null — the cooperative scheduler's decision history"},
    {"task_self", "task_self of null → the running task's id (a number, in the same integer"},
    {"task_send", "task_send of [id, value] — append a deep-copied message to task `id`'s"},
    {"task_sleep", "task_sleep of ticks — suspend the current task until the virtual clock"},
    {"task_spawn", "task_spawn of fn  /  task_spawn of [fn, arg1, ...] → task id (a number)."},
    {"task_try_recv", "task_try_recv of null — non-blocking receive: the next message, or null if"},
    {"task_yield", "task_yield of null — cooperatively hand control to the next ready task."},
    {"tensor_load", "tensor_load of path — load 1D or 2D tensor from binary file."},
    {"tensor_save", "tensor_save of [tensor, path] — save 1D or 2D list to binary file."},
    {"text_builder_append", "text_builder_append — builtin; see docs/BUILTINS.md"},
    {"text_builder_append_line", "text_builder_append_line — builtin; see docs/BUILTINS.md"},
    {"text_builder_clear", "text_builder_clear — builtin; see docs/BUILTINS.md"},
    {"text_builder_extend", "text_builder_extend — builtin; see docs/BUILTINS.md"},
    {"text_builder_new", "text_builder_new — builtin; see docs/BUILTINS.md"},
    {"text_builder_part_count", "text_builder_part_count — builtin; see docs/BUILTINS.md"},
    {"text_builder_to_string", "text_builder_to_string — builtin; see docs/BUILTINS.md"},
    {"thread_join", "thread_join — builtin; see docs/BUILTINS.md"},
    {"throw", "throw — builtin; see docs/BUILTINS.md"},
    {"token_name", "token_name of id → string name of token type (for display)"},
    {"tokenize_ids", "tokenize_ids of string → list of token type IDs (integers)."},
    {"tokenize_with_names", "tokenize_with_names of string → list of [type_id, name_str] pairs."},
    {"trim", "trim — builtin; see docs/BUILTINS.md"},
    {"try_parse", "try_parse of string → 1 if valid EigenScript syntax, 0 if not."},
    {"try_recv", "try_recv of channel — non-blocking receive, returns null if empty"},
    {"type", "type — builtin; see docs/BUILTINS.md"},
    {"usleep", "usleep of microseconds — pause execution"},
    {"values", "values — builtin; see docs/BUILTINS.md"},
    {"vm_run_bytecode", "vm_run_bytecode of <chunk-descriptor> — assemble a chunk (and its nested"},
    {"write", "write of value — output without trailing newline"},
    {"write_bytes", "write_bytes of [path, data, append?] — write raw bytes to a file."},
    {"write_text", "write_text of [\"path\", text] → 1 on success, 0 on failure."},
    {"zeros", "zeros of n → 1D list of n zeros"},
    {"zeros_like", "zeros_like — builtin; see docs/BUILTINS.md"},
    {"zlib_deflate", "zlib_deflate — builtin; see docs/BUILTINS.md"},
    {"zlib_inflate", "zlib_inflate — builtin; see docs/BUILTINS.md"},
    {0, 0}
};
